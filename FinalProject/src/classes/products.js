export default class Products {

     constructor(timestamp, name, description, code, photo,  price, stock) {
         this.timestamp = timestamp;
         this.name = name;
         this.description = description;
         this.code = code;
         this.photo = photo;
         this.price = price;
         this.stock = stock;
     }
 }